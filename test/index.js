'use strict';

var assert = require('assert');
var paraCli = require('../para-cli.js');

describe('para-cli', function () {
  it('TODO!', function () {
    assert(paraCli !== null, 'we expected this package author to add actual unit tests.');
  });
});
